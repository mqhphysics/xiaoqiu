# 晓球多 AI 协作规范

## 必读

开始任务前依次阅读：

1. `docs/implementation/晓球方案A详细实施方案v0.2.md`
2. 本文件
3. `docs/tasks/` 中分配给你的任务卡

## 工作原则

- 只完成被分配的任务，不扩展首发范围。
- 不自行改变公共数据模型、API 契约源和技术路线。
- 不修改其他 Agent 的独占写入范围。
- 不撤销或覆盖已有修改。
- 发现架构歧义时记录问题并交由集成负责人决定。
- 所有重要写操作遵守组织隔离、权限、幂等、审计和版本规则。

## 公共文件所有权

以下文件默认只由集成负责人修改：

```text
package.json
pnpm-workspace.yaml
pnpm-lock.yaml
tsconfig.base.json
eslint.config.mjs
prisma/schema.prisma
packages/contracts/
packages/api-client/
```

任务卡可以将其中某个文件或目录明确授予单个 Agent 独占修改；该明确授权在任务期间优先于默认所有权。没有明确授权时，只在任务结果中列出建议，不直接越权修改。

## 分支与依赖规则

- 每个并行任务使用独立分支和独立 worktree。
- 分支命名遵循任务卡，例如 `ai/p0-api-database`。
- Agent 不在自己的分支中修改 `pnpm-lock.yaml`。
- 如需新增或升级依赖，可修改自己独占范围内的 `package.json`，并在完成报告中列出依赖变化；由集成负责人统一执行安装并更新锁文件。
- 不合并其他并行分支，也不处理不属于本任务的冲突。

## 完成报告

每个任务完成后必须列出：

- 修改文件。
- 已满足的验收标准。
- 执行的命令和结果。
- 未解决问题。
- 公共文件变更建议。
- 需要集成负责人协调的事项。

## 禁止事项

- 不在 `main` 上进行并行开发。
- 不提交真实密钥和个人信息。
- 不直接修改生成的 API Client。
- 不仅凭姓名匹配或覆盖球员档案。
- 不绕过应用服务直接实现管理员修正。
- 不把前端隐藏按钮视为权限控制。
