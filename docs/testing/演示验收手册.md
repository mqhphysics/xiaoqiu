# 晓球 V1 演示验收手册

> 本手册用于本地演示验收。赛事、账号和统计均为 `DEMO_FIXTURE` 虚构数据，不得当作真实学生信息或生产账号使用。

## 1. 当前演示内容

- 2026 绿茵杯：8 支球队、2 个小组、16 场比赛、112 名完整球员。
- 同时包含已结束、进行中、未开始和延期比赛，以及比分、出场、进球、助攻和红黄牌。
- 2025 历史赛季：8 场淘汰赛，用于验证赛季切换。
- 6 条官方/社区内容及其点赞、评论。
- 首页、赛程、数据、主队、我的五个核心入口，以及比赛、球队、球员、动态详情。

## 2. 首次启动

从仓库根目录执行。先准备并启动 PostgreSQL：

```powershell
Copy-Item infra/.env.example infra/.env
docker compose --env-file infra/.env -f infra/compose.yaml up -d postgres
docker compose --env-file infra/.env -f infra/compose.yaml ps
```

在同一个 PowerShell 窗口初始化数据库并启动 API：

```powershell
$env:DATABASE_URL = 'postgresql://xiaoqiu:xiaoqiu-local-only@localhost:5432/xiaoqiu'
pnpm --filter @xiaoqiu/api db:migrate:deploy
pnpm --filter @xiaoqiu/api db:seed
pnpm dev:api
```

看到 Nest application successfully started 后保留窗口。在第二个 PowerShell 窗口启动 H5：

```powershell
pnpm dev:h5
```

访问：

- H5：`http://127.0.0.1:10087/`
- API 健康：`http://127.0.0.1:3001/api/health/ready`
- API 文档：`http://127.0.0.1:3001/api/docs`

Seed 可以重复执行。需要恢复初始演示内容时，停止 API 后重新执行 `db:seed`。

## 3. 演示账号

所有账号使用共同密码：`Xiaoqiu2026!`

| 用户名     | 昵称       | 实名   | 学号          | 用于体验                     |
| ---------- | ---------- | ------ | ------------- | ---------------------------- |
| `student`  | 知夏看球   | 林知夏 | `20249990001` | 普通浏览、主队关注、动态互动 |
| `player`   | 星野       | 高星野 | `20248800011` | 已认领球员档案               |
| `captain`  | 明澈队长   | 郑明澈 | `20248800007` | 球员身份与球队队长权限       |
| `reporter` | 嘉言现场   | 沈嘉言 | `20239990002` | 比赛信息员入口               |
| `admin`    | 清越赛事组 | 韩清越 | `20219990003` | 实名目录、赛事与组织管理入口 |

登录页可直接点击任一身份卡片，用户名和密码会自动填入；也可以使用该账号的用户名、昵称、实名或学号登录。邮箱验证码入口在 V1 中只展示交互，不发送真实邮件。

## 4. 建议验收路线

### 4.1 首页与搜索

1. 打开首页，应看到 `2026 绿茵杯 · 演示赛季`、一场 `1:1` 进行中比赛、官方公告和焦点赛程。
2. 搜索 `物院`，应出现物院球队、相关比赛或动态。
3. 切换到“球员”并搜索 `高星野`，进入后应看到号码、位置、惯用脚、年级专业、简介和赛季统计。
4. 点击焦点比赛，应看到比分、事件时间轴和双方阵容；阵容中的球员可继续进入详情。

### 4.2 赛程与数据

1. 进入“赛程”，分别选择全部、未开始、已结束，并切换日期正序/倒序。
2. 已结束比赛必须显示比分，进行中比赛显示状态，未来比赛显示时间和场地。
3. 进入“数据”，确认概览显示 8 队、16 场、7 场已结束和 1 场进行中。
4. 积分榜应显示完整球队名和积分；射手榜、助攻榜可进入球员档案。
5. 查看淘汰赛路径，再切换到 2025 赛季，确认历史数据能够独立展示。

### 4.3 主队与完整名单

1. 未登录进入“主队”，应看到 8 支球队和登录提示。
2. 使用 `student` 登录后再次进入，默认主队为“数院星火”，并显示 14 人完整阵容。
3. 点击“管理关注”，更换主队或关注队伍后保存；刷新页面，选择仍应保留。
4. 任意进入一支球队，检查简介、口号、教练、队长、战绩、近期比赛和 14 人名单。
5. 任意点击普通球员，出场次数、首发次数和比赛记录必须一致，不能只给进球者显示统计。

### 4.4 登录与社区写入

1. 使用 `student`、`知夏看球`、`林知夏`、`20249990001` 分别登录，四种标识都应进入同一账号。
2. 在“我的”依次体验五个账号，确认昵称、实名、完整本人学号、身份标签、关联球员和角色入口不同。
3. 使用 `student` 查看实名目录应被拒绝；使用 `admin` 应看到本组织五个账号的完整学号和邮箱。
4. 点击“忘记密码”，使用林知夏和 `20249990001` 设置新密码；验收后重新 Seed 可恢复公共演示密码。
5. 登录后回到首页，点赞一条动态并刷新，点赞状态应保留。
6. 发布一条测试动态，进入详情后发表评论，刷新后内容仍存在。
7. 退出登录后，发布、点赞和评论应要求重新登录。

### 4.5 响应式与异常状态

1. 桌面浏览器检查五项顶部导航；窄屏检查五项底部 Tab 均完整可点。
2. 手机宽度下不得出现页面级横向滚动、比分卡片裁切或文字覆盖。
3. 暂停 API 后刷新，应显示明确错误与“重新加载”，不能静默退回 Mock 数据。

## 5. 微信开发者工具

先构建小程序：

```powershell
pnpm --filter @xiaoqiu/mini-program build
```

在微信开发者工具中导入 `apps/mini-program`。项目配置已将 `dist/` 设为小程序目录，并使用测试 AppID。模拟器访问本机 API 时保持 API 在 `3001` 端口运行，并关闭开发阶段的合法域名校验。

真机不能用手机自己的 `127.0.0.1` 访问电脑。真机联调时需把 `TARO_APP_API_BASE_URL` 设置为电脑局域网地址后重新构建，并允许 Windows 防火墙访问该端口。V1 发布前还需要单独完成开发者工具和真机验收。

## 6. 自动检查

```powershell
pnpm --filter @xiaoqiu/api typecheck
pnpm --filter @xiaoqiu/api test
pnpm --filter @xiaoqiu/mini-program typecheck
pnpm --filter @xiaoqiu/mini-program test
pnpm --filter @xiaoqiu/mini-program build:h5
pnpm --filter @xiaoqiu/mini-program build
```

H5 首屏图片和开发构建体积目前会产生非阻断警告；功能、类型或测试错误不得忽略。
